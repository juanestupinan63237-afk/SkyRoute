class SimulationEngine:
    #Metodos de Simulación:
    """
    run(traveler: Traveler, graph: Graph) -> None
    offer_activities(node: Node) -> list[Actividad]
    offer_jobs(node: Node) -> list[Trabajo]
    choose_flight(opciones: list[Edge]) -> Edge
    """